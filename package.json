{
    "name": "engineer-dao-platform",
    "version": "1.0.0",
    "author": "EngineerDAO",
    "license": "MIT",
    "private": true,
    "workspaces": [
        "frontend",
        "backend",
        "contracts"
    ],
    "scripts": {
        "build": "yarn lerna run build",
        "lint": "yarn lerna run lint",
        "bootstrap": "yarn lerna run bootstrap",
        "clean": "yarn lerna run clean --parallel",
        "test": "yarn lerna run test --parallel"
    },
    "dependencies": {},
    "devDependencies": {
        "lerna": "^4.0.0",
        "typescript": "^4.5.5",
        "ts-node": "^10.0.0"
    }
}
