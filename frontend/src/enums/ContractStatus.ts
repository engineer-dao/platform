export enum ContractStatus {
  available = 'Available',
  active = 'Active',
  awaiting_payment = 'Awaiting Payment',
  completed = 'Completed',
  disputed = 'Disputed',
  close_requested = 'Close Requested',
  closed = 'Closed',
}
