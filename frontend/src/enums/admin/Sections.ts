export enum SectionPath {
  dashboard = '/',
  contracts = '/contracts',
  myContracts = '/my-contracts',
  community = '/community',
  contract = '/contract/:id',
  createContract = '/create-contract',
}
